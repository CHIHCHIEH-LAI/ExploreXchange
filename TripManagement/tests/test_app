import pytest

import os
import requests

def get_base_uri():
    return 'http://localhost:8001'

def test_clean_collection():
    base_uri = get_base_uri()
    uri = os.path.join(base_uri, 'trips/create-trip')
    response = requests.get(uri)
    assert response.status_code == 200